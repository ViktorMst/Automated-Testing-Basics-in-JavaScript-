// Write a function for calculating the type of argument and type output to the console//
function myFn(arg) {
  console.log(`The argument is a type of: ${typeof arg}`);
}

myFn("abc");
myFn(55);
myFn(false);
